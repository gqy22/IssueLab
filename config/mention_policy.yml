# @ 提及策略配置
# 集中管理所有 @mention 相关策略

mention_policy:
  # 黑名单：禁止 @ 的用户（即使在正文中提到，也不会在 @ 区域出现）
  blacklist: []
    # - spam-user
    # - bot-account

  # 频率限制（内存态实现，重启后计数重置）
  rate_limit:
    enabled: false
    max_per_issue: 10  # 每个 Issue 最多 @ 10 次
    max_per_hour: 5    # 每小时最多 @ 5 次
